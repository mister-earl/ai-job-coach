import React from 'react';
import ReactDOM from 'react-dom/client';
import CoachV2 from './coach-v2-artifact';

const root = ReactDOM.createRoot(document.getElementById('root'));
root.render(
  <React.StrictMode>
    <CoachV2 />
  </React.StrictMode>
);
